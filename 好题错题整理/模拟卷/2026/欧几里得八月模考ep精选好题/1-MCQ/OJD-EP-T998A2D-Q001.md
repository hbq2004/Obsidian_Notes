---
ctime: 2026-08-15 19:15:19
mtime: 2026-08-20 01:07:44
tags:
  - AM
  - AM/精选好题
  - 证明题
  - 凸函数
  - 拉格朗日中值定理
  - 数列收敛
  - 单调数列
points:
level:
---

# MCQ 第 1 题

![[_Attachments/题目识别/OJD-EP/OJD-EP-T998A2D-Q001_题目.png|题目]]

【例1】设函数 $f(x)$ 在 $[0, +\infty)$ 上可导，$f(0)=0$，且 $f'(x)$ 严格单调递增。令 $u_n = \frac{f(n)}{n}$，$v_n = f(n+1)-f(n)$ $(n=1,2,\cdots)$，则数列 $\{u_n\}$ 收敛是数列 $\{v_n\}$ 收敛的（）

A. 充分非必要条件. B. 必要非充分条件. C. 充分必要条件. D. 既非充分也非必要条件.

---

## 答案与解析

**答案**：**C. 充分必要条件.**

**切入点**（把数列看成"斜率"）：$f'(x)$ 严格递增 ⟹ $f$ 严格凸。这时

- $u_n=\dfrac{f(n)}{n}=\dfrac{f(n)-f(0)}{n-0}$ = 连接 $(0,0)$ 与 $(n,f(n))$ 的**割线斜率**；
- $v_n=f(n+1)-f(n)$ = 相邻两点的**割线斜率**（由拉格朗日中值定理 $v_n=f'(\xi_n)$，$\xi_n\in(n,n+1)$）。

凸函数的割线斜率随区间右移而递增，所以 $\{u_n\}$、$\{v_n\}$ 都**单调递增**——单调数列收敛 ⟺ 有上界，问题转化为"有界性能否互相传递"。

**推演**：

1. $v_n$ 的单调性：由 MVT，$v_n=f'(\xi_n)$（$\xi_n\in(n,n+1)$），$f'$ 严格递增 ⟹ $v_n$ 严格递增。
   $u_n$ 的单调性：令 $g(x)=\dfrac{f(x)}{x}$，由 $f$ 严格凸且 $f(0)=0$ 有 $f(x)=\int_0^x f'(t)\,\mathrm{d}t < xf'(x)$，故 $g'(x)=\dfrac{xf'(x)-f(x)}{x^{2}}>0$，即 $g$ 严格递增 ⟹ $u_n=g(n)$ 严格递增。
2. 望远镜求和：$f(n)=\sum_{k=0}^{n-1}v_k$（记 $v_0=f(1)-f(0)=f(1)$），于是

$$u_n=\frac{1}{n}\sum_{k=0}^{n-1}v_k,$$

即 $u_n$ 是 $\{v_k\}$ 的**前缀平均**（和算法里的前缀和/滑动平均同构）。
3. 对递增数列 $\{v_k\}$：$u_n\le v_{n-1}\le v_n$，且 $u_n\ge v_0$。
4. **($\impliedby$) 必要方向**：若 $\{v_n\}$ 收敛则有上界 $M$，由 $u_n\le v_n\le M$ 且 $\{u_n\}$ 递增 ⟹ $\{u_n\}$ 收敛。
5. **($\implies$) 充分方向**：若 $\{v_n\}$ 发散，因它递增必 $v_n\to+\infty$，则后一半项都不小于 $v_{\lfloor n/2\rfloor}$：

$$u_n=\frac{1}{n}\sum_{k=0}^{n-1}v_k\ \ge\ \frac{1}{n}\cdot\frac{n}{2}\cdot v_{\lfloor n/2\rfloor}=\frac{v_{\lfloor n/2\rfloor}}{2}\to+\infty,$$

即 $\{u_n\}$ 也发散。逆否：$\{u_n\}$ 收敛 ⟹ $\{v_n\}$ 收敛。

两方向都成立 ⟹ **充要条件**，选 **C**。

**易错点**：

1. 只由 $u_n\le v_n$ 推出"$v$ 收敛 ⟹ $u$ 收敛"（单方向）就选 B——**漏了反方向**：递增数列发散必 $+\infty$，而 $u_n\ge \frac12 v_{\lfloor n/2\rfloor}$ 把发散性传回去；
2. 有人想用 Stolz 直接算 $\lim u_n=\lim v_n$——注意 Stolz 要分子分母同阶趋于 $\infty$，这里 $f(n)$ 未必 $\to\infty$，不能直接用；"单调 + 有界"才是安全的判据；
3. $u_n$ 单调性来自凸性（$f(x)/x$ 递增），不是 $u_{n+1}-u_n$ 硬算出来的。

**命题规律**："凸性 ⟹ 割线斜率递增" 是数列题和中值定理之间的桥；$\left\{\dfrac{f(n)}{n}\right\}$ 与 $\{f(n+1)-f(n)\}$ 的收敛等价，本质是 **Cesàro 平均与增量的互推**，这类"平均 ⟺ 增量"的充要题数一反复考，记住"递增数列发散必趋于 $+\infty$"这把钥匙。




---

## V4 Flash Max 解析

**答案**: C（充分必要条件）。令 $w_k=f(k)-f(k-1)$，则 $u_n$ 是 $w_1,\dots,w_n$ 的算术平均，$v_n=w_{n+1}$；且由 $f'(x)$ 严格单调递增可知 $\{w_k\}$ 单调递增。单调数列与其算术平均同敛散，所以两方向都成立。

**解题切入点**:  
不要直接对 $u_n$、$v_n$ 做不等式。先把 $f(n)$ 拆成相邻增量之和，则 $u_n$ 变成“前缀平均值”，$v_n$ 变成“当前差分”。算法竞赛里可类比：一个单调数组的均值收敛等价于数组本身收敛；关键是单调性。

**推演**:  
令
$$
w_k=f(k)-f(k-1)\quad (k=1,2,\dots).
$$
由 $f(0)=0$，有
$$
f(n)=\sum_{k=1}^n\bigl(f(k)-f(k-1)\bigr)=\sum_{k=1}^n w_k,
$$
所以
$$
u_n=\frac{f(n)}n=\frac1n\sum_{k=1}^n w_k,\qquad v_n=f(n+1)-f(n)=w_{n+1}.
$$

再证 $\{w_k\}$ 单调递增。由拉格朗日中值定理，存在
$$
\xi_k\in(k-1,k),\qquad \xi_{k+1}\in(k,k+1)
$$
使得
$$
w_k=f'(\xi_k),\qquad w_{k+1}=f'(\xi_{k+1}).
$$
因为 $\xi_k<\xi_{k+1}$，且 $f'$ 严格单调递增，所以
$$
w_k=f'(\xi_k)<f'(\xi_{k+1})=w_{k+1}.
$$
故 $\{w_k\}$ 单调递增。

下面用单调数列与其算术平均的同敛散结论：

- 若 $w_k\to W$，则
  $$
  u_n=\frac1n\sum_{k=1}^n w_k\to W,
  $$
  即 $u_n$ 收敛。

- 反过来，若 $u_n$ 收敛而 $\{w_k\}$ 不收敛，由于 $\{w_k\}$ 单调递增，必有无上界，从而 $w_k\to+\infty$。此时对任意 $M>0$，存在 $N$ 使 $w_N\ge M$，于是
  $$
  u_n\ge \frac{1}{n}\left(\sum_{k=1}^{N-1}w_k+(n-N+1)w_N\right)\to w_N\ge M,
  $$
  因此 $\liminf\limits_{n\to\infty}u_n\ge M$。由 $M$ 任意，$u_n\to+\infty$，与 $u_n$ 收敛矛盾。故 $\{w_k\}$ 必收敛。

所以
$$
u_n\text{ 收敛}\iff w_k\text{ 收敛}\iff v_n=w_{n+1}\text{ 收敛}.
$$

因此 $\{u_n\}$ 收敛是 $\{v_n\}$ 收敛的充分必要条件。

**易错点**:  
不要把它当成一般数列的“平均值与差分”关系；一般数列中平均值收敛推不出差分收敛，这里关键是由 $f'$ 单调得到的 $\{w_k\}$ 单调。  
也不要只记住 $u_n\le v_n$：这个不等式只直接说明 $v_n$ 有界时 $u_n$ 有界；反方向必须用“平均值有界推出单调差分有界”。  
自检：取 $f(x)=x+e^{-x}-1$，则 $u_n\to1$，$v_n\to1$；取 $f(x)=e^x-x-1$，则两者都趋于 $+\infty$，不出现一敛一散。

**命题规律**:  
本题是“凸函数 + 数列极限”的典型题。$f'$ 单调递增等价于 $f$ 为凸函数，$f(n)$ 可看成凸离散序列；$u_n$ 是原点割线斜率，$v_n$ 是相邻差分。常见变式：判断 $u_n$ 与 $v_n$ 的大小、比较 $u_n$ 与 $u_{n+1}$、用 Stolz/Cesàro 平均求极限，也可与拉格朗日中值定理、洛必达法则结合。