---
mtime: 2026-07-23 20:09:21
---
<%*
let sel = tp.file.selection();
if (!sel) {
    new Notice("请先用鼠标选中整个图片链接！");
} else {
    // 自动清除首尾的隐形空格，防止匹配失败
    sel = sel.trim();
    let cleaned = sel.replace(/\|题目\]\]$/, "]]").replace(/\|答案\]\]$/, "]]");
    tR += cleaned.replace(/\]\]$/, "|题目]]");
}
%>