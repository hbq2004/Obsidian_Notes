---
ctime: 2026-09-26 19:00:29
mtime: 2026-09-26 20:09:11
---


可以，给你 10 个混合题，数学一 + 408，难度大概按考研正常训练水平来，不给答案。

1. **数学一｜极限**  
    计算
    
    \[ \lim_{x\to 0}\frac{e^x-1-x-\frac{x^2}{2}}{x^3}. \]
2. **数学一｜多元微分**  
    设
    
    \[ z=x^2y+e^{xy}, \]
    
    求 \(dz\)，并求点 \((1,0)\) 处沿方向 \((3,4)\) 的方向导数。
    
3. **数学一｜二重积分**  
    计算
    
    \[ \iint_D (x^2+y^2)\,dxdy, \]
    
    其中
    
    \[ D=\{(x,y)\mid x^2+y^2\le 2x\}. \]
4. **数学一｜线性代数**  
    已知矩阵
    
    \[ A= \begin{pmatrix} 1&1&0\\ 1&1&0\\ 0&0&2 \end{pmatrix}, \]
    
    求 \(A\) 的全部特征值，并判断 \(A\) 是否可以相似对角化。
    
5. **数学一｜概率论**  
    设随机变量
    
    \[ X\sim N(2,9), \]
    
    求
    
    \[ P(-1<X<5), \]
    
    并将答案化为标准正态分布函数 \(\Phi(x)\) 的形式。
    
6. **408｜数据结构**  
    一个长度为 \(n\) 的单链表，若只给出头结点指针，要求删除链表中的**倒数第 \(k\) 个结点**。  
    如何设计一个只遍历链表一次、空间复杂度为 \(O(1)\) 的算法？说明核心思想。
    
7. **408｜计算机组成原理**  
    某计算机主存按字节编址，Cache 采用直接映射方式。  
    Cache 共 64 行，每行 32B，主存地址为 32 位。  
    问：地址中的 **Tag、Cache 行号、块内地址** 分别占多少位？
    
8. **408｜操作系统**  
    某系统有三个进程 \(P_1,P_2,P_3\)，采用时间片轮转调度算法，时间片为 2ms。  
    三个进程同时到达，其 CPU 执行时间分别为：
    
    \[ 3ms,\quad 5ms,\quad 7ms. \]
    
    假设进程切换时间忽略不计，求三个进程各自的完成时间。
    
9. **408｜计算机网络**  
    主机 A 向主机 B 发送一个长度为 2000B 的 IP 数据报。链路 MTU 为 800B，IP 首部长度为 20B。  
    问该数据报需要分成几个 IP 分片？每个分片的数据部分长度分别是多少？
    
10. **408｜综合理解**  
    假设一个程序执行时发生了一次缺页。请按照时间顺序说明从 **CPU 访问虚拟地址 → 页表查询 → 发现缺页 → 操作系统处理 → 页面重新访问成功** 的完整过程，并说明 TLB 在其中起什么作用。




$O(\log n)$ 

